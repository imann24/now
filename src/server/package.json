{
    "name": "now-server",
    "version": "1.0.0",
    "scripts": {
        "start": "node src/server.js",
        "test": "jest --verbose"
    },
    "dependencies": {
        "body-parser": "^1.18.3",
        "express": "^4.16.4",
        "socket.io": "^2.2.0"
    },
    "eslintConfig": {
        "parserOptions": {
            "ecmaVersion": 2017
        },
        "env": {
            "es6": true
        }
    },
    "devDependencies": {
        "jest": "^24.7.1"
    }
}
