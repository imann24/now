{
    "name": "now-server",
    "version": "1.0.0",
    "scripts": {
        "start": "node src/server.js",
        "dev": "nodemon -r dotenv/config src/server.js",
        "test": "jest --verbose"
    },
    "dependencies": {
        "body-parser": "^1.18.3",
        "dotenv": "^8.1.0",
        "express": "^4.16.4",
        "socket.io": "^2.2.0",
        "twilio": "^3.35.1"
    },
    "eslintConfig": {
        "parserOptions": {
            "ecmaVersion": 2017
        },
        "env": {
            "es6": true
        }
    },
    "devDependencies": {
        "jest": "^24.7.1",
        "nodemon": "^1.19.4"
    }
}
